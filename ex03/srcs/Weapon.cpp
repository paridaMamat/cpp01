#include "Weapon.hpp"
Weapon weapon;
weapon.getType(weapon.type){
    const std::string &RType;
    RType = weapon.type;
    return (RType);
}

weapon.setType(std::string newWeapon){
    this.type = newWeapon;
}